{
  "$schema": "https://schema.railway.com/railway.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && PUPPETEER_CACHE_DIR=/app/.cache/puppeteer npx puppeteer browsers install chrome"
  },
  "deploy": {
    "startCommand": "npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
